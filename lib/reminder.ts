/** Client reminder polling interval (ms). */
export const REMINDER_CHECK_INTERVAL_MS = 30_000;

/** Default snooze duration for in-app reminders. */
export const REMINDER_SNOOZE_MS = 10 * 60 * 1000;
